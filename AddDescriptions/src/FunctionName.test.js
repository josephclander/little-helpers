const functionName = require("./FunctionName");
const testData = require("./testData");

describe("", () => {
  it("", () => {
    expect(functionName(testData.test1)).toEqual(
      JSON.stringify(testData.test1).toString()
    );
  });
});
