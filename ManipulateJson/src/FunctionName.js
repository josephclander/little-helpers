/**
 * Convert JSON into the Cyclr output
 * @param {object} input
 * @return {json}
 */
function functionName(input) {
  /*
    Required output for Cyclr fields
      {
        "Values": [],
        "ConnectorField": "string",
        "SystemField": "String",
        "DataType": "Text",
        "Description": "String",
        "DisplayName": "String",
        "DisplayOrder": 1
      }
  */
 

  return JSON.stringify(output);
}

module.exports = functionName;
